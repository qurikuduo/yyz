{
  "name": "depression-selftest",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-based depression self-assessment web app (PHQ-9/PHQ-2, SDS, CES-D, BDI-II, DSM-5) with comprehensive synthesis, bilingual UI, shareable password-protected results, and a self-help knowledge base.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server run start"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
